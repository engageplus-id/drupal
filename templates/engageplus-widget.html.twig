{#
/**
 * @file
 * Default theme implementation for the EngagePlus widget.
 *
 * Available variables:
 * - container_id: The unique container ID for the widget.
 * - org_id: The EngagePlus organization ID.
 * - config: Widget configuration array.
 *
 * @ingroup themeable
 */
#}
<div class="engageplus-widget-container {{ config.customCssClass|default('') }}">
  <div class="engageplus-widget-wrapper">
    <div id="{{ container_id }}"></div>
  </div>
</div>

